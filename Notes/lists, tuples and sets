name    What punctuation   ordered      changeable      duplicates allowed   types of data stored in them    what methods can be used on them
Lists:       []              yes            yes               yes                       any                         append(adds item to end of list), clear(allows you to remove all items from list), count(counts the # of items), index(lets you know where the idexed thing is), insert(allows you to add a item at a specific location), pop(lets you remove an item with a specified value), reverse(allows you to reverse th list/flip it), sort(if not specified sotrs in aplebtical, if specified srts in specific way) 
Tuples:      ()              yes            no                yes                       any                         count(counts the # of items), index(lets you know where the idexed thing is)
Sets:        {}              no     no but add and remove     no                        any                         pop(removes random item from set), discard(removes a specific item), add(adds a item in a random position into it), remove(removes a specific item)